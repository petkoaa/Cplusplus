#include <iostream>
#include <vector>

using namespace std;

int main()
{
   string a;
    std::vector<int> vec();
    int len = a.length();
    for (int i= 0; i<len; i++){
        int c = (int)(a[i]);
        vec.insert(c) ;
    }
   return 0;
}
